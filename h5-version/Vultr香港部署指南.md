# 🚀 Vultr香港VPS部署指南

## 为什么选Vultr香港？

| 优势 | 说明 |
|------|------|
| ✅ 无需备案 | 香港服务器，国内法律不适用 |
| ✅ 微信完美 | 移动网络、微信内都流畅访问 |
| ✅ 按需付费 | 用完可以删除，只花几毛钱 |
| ✅ 国内速度快 | 香港到大陆延迟低 |
| ✅ 支付宝 | 支持支付宝付款 |
| ✅ 5分钟部署 | 从注册到上线5分钟搞定 |

---

## 步骤1：注册Vultr（2分钟）

### 访问注册
```
https://www.vultr.com/
```

### 注册账号
- Email：填写邮箱
- Password：设置密码
- Email Verification：验证邮箱

### 充值（支持支付宝）
```
Billing → Make Payment
金额：$10（够用2个月）
支付方式：Alipay（支付宝）
扫码支付：约¥70
```

---

## 步骤2：创建服务器（3分钟）

### 部署新服务器
```
1. 点击右上角 "+" （Deploy Server）
2. 选择配置：
   - Server Location：Hong Kong
   - Server Type：Ubuntu 22.04 x64
   - Server Size：$5/mo（512MB或1GB）
   - Server Hostname：wedding-defender
3. 点击"Deploy Now"
```

### 等待创建
```
等待30秒-1分钟
状态变为"Running"
获得IP地址：如 203.0.113.1
```

---

## 步骤3：连接服务器（1分钟）

### 方式A：Vultr网页控制台
```
服务器列表 → 点击"..."
选择"Launch Console"
用户名：root
密码：查看邮件（Vultr会发）
```

### 方式B：SSH客户端（推荐）
```bash
# Mac/Linux
ssh root@203.0.113.1

# Windows（用PuTTY）
Host: 203.0.113.1
Port: 22
Username: root
Password: （查看邮件）
```

### 首次登录会要求改密码
```
Current password: [输入初始密码]
New password: [设置新密码]
Retype new password: [再次输入]
```

---

## 步骤4：安装Nginx（2分钟）

### 更新系统
```bash
apt update
apt upgrade -y
```

### 安装Nginx
```bash
apt install nginx -y
```

### 启动Nginx
```bash
systemctl start nginx
systemctl enable nginx
```

### 测试访问
```bash
# 浏览器访问
http://你的服务器IP

# 能看到"Welcome to nginx!"说明成功
```

---

## 步骤5：部署网站（2分钟）

### 方式A：直接创建（最简单）
```bash
# 进入网站目录
cd /var/www/html

# 删除默认页面
rm index.nginx-debian.html

# 创建新的index.html
nano index.html

# 把h5-version/index.html的内容粘贴进去
# 保存：Ctrl+O → Enter → Ctrl+X
```

### 方式B：上传文件
```bash
# 在你的本地电脑执行
scp -r /Users/你的路径/h5-version/* root@203.0.113.1:/var/www/html/

# 或者用SFTP工具（FileZilla等）
# 文件协议：SFTP
# 主机：203.0.113.1
# 用户：root
# 密码：你的服务器密码
# 端口：22
# 上传h5-version所有文件到 /var/www/html/
```

### 方式C：Git拉取（如果你把代码推送到GitHub）
```bash
cd /var/www/html
rm -rf *
apt install git -y
git clone https://github.com/你的用户名/wedding-defender.git .
# 或者用Gitee
git clone https://gitee.com/你的用户名/wedding-defender.git .
```

---

## 步骤6：访问测试（1分钟）

### 浏览器访问
```
http://你的服务器IP

应该能看到催婚防御助手页面
```

### 微信测试
```
1. 复制链接
2. 微信里粘贴发送
3. 点击打开
4. ✅ 成功访问
```

---

## 步骤7：绑定域名（可选，10分钟）

### 购买域名
```
推荐：阿里云、腾讯云
搜索：chunyuan-mianqiang.com 等
购买：.com约50-70元/年
```

### DNS解析
```
在域名控制台添加A记录：
类型：A
主机记录：@
记录值：你的服务器IP
TTL：600
```

### Nginx配置域名
```bash
# 编辑Nginx配置
nano /etc/nginx/sites-available/default

# 找到server_name，改为：
server_name 你的域名.com www.你的域名.com;

# 保存后重启
systemctl restart nginx
```

### 访问
```
http://你的域名.com
```

---

## 步骤8：配置HTTPS（可选，5分钟）

### 安装Certbot
```bash
apt install certbot python3-certbot-nginx -y
```

### 申请证书
```bash
certbot --nginx -d 你的域名.com -d www.你的域名.com
```

### 自动续期
```bash
certbot renew --dry-run
```

### 完成
```
https://你的域名.com
```

---

## 💰 费用说明

| 项目 | 金额 |
|------|------|
| 按小时计费 | $0.007/小时 |
| 按天计费 | $0.17/天 |
| 按月计费 | $5/月 |

### 使用场景

**春节用1周：**
```
$0.007 × 24小时 × 7天 = $1.18（约¥8）
```

**用完就删：**
```
1. 销毁服务器：Vultr控制台 → Server → Destroy
2. 停止计费
3. 数据无法恢复（注意备份）
```

**持续使用：**
```
$5/月 = 约¥35/月
随时可以销毁停止计费
```

---

## 🛠️ 常用命令

### 查看Nginx状态
```bash
systemctl status nginx
```

### 重启Nginx
```bash
systemctl restart nginx
```

### 查看网站日志
```bash
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### 更新网站文件
```bash
cd /var/www/html
nano index.html
# 或重新上传
```

---

## ⚠️ 注意事项

### 安全
```bash
# 修改SSH端口（可选）
nano /etc/ssh/sshd_config
# Port 22 改为其他端口
systemctl restart sshd

# 配置防火墙（可选）
ufw allow 80
ufw allow 443
ufw allow 22（或你的SSH端口）
ufw enable
```

### 备份
```bash
# 定期备份网站文件
tar -czf backup.tar.gz /var/www/html
# 下载到本地
scp root@IP:/root/backup.tar.gz ./
```

### 监控流量
```
Vultr控制台 → Server
查看：带宽使用、CPU使用
$5套餐：1TB流量/月
```

---

## 🚀 性能优化

### 开启Gzip（已默认）
```bash
nano /etc/nginx/nginx.conf
# 确认以下配置：
gzip on;
gzip_types text/plain text/css application/json application/javascript;
```

### 配置缓存
```bash
nano /etc/nginx/sites-available/default

# 在server块添加：
location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### 重启Nginx
```bash
systemctl restart nginx
```

---

## 📱 测试清单

- [ ] 电脑浏览器访问IP
- [ ] 手机浏览器访问IP
- [ ] 微信内访问IP
- [ ] 4G/5G网络访问
- [ ] （可选）域名访问
- [ ] （可选）HTTPS访问

---

## 🎯 完成时间线

| 步骤 | 耗时 |
|------|------|
| 注册+充值 | 2分钟 |
| 创建服务器 | 3分钟 |
| 连接服务器 | 1分钟 |
| 安装Nginx | 2分钟 |
| 部署网站 | 2分钟 |
| 测试访问 | 1分钟 |
| **总计** | **13分钟** |

---

## 🎉 完成！

你的催婚防御助手已经上线：
```
http://你的服务器IP
或
http://你的域名.com（如果配置）
```

### 下一步
1. 生成二维码
2. 分享到微信群
3. 春节救急！

---

## 需要帮助？

- Vultr文档：https://www.vultr.com/docs/
- Nginx文档：https://nginx.org/en/docs/

**春节快乐，优雅应对催婚！** 🍀
